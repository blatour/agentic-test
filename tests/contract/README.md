# Contract Tests

This folder is owned primarily by Agent F with inputs from all workstreams.

Required first tests:

1. Canonical envelope schema validation.
2. KnowledgeState schema validation.
3. ChangeSet schema validation.
4. ActionDecision schema validation.
5. Plugin adapter interface conformance checks.
